$(document).ready(function() {
	/*$('#team-search select').selectric();
	
	$('#team-search input[type="submit"]').on('click', function(e) {
		var teamQuery = $('#team-search input[type="text"]').val();
		if (teamQuery) {
			var query = "http://api.cartola.globo.com/time/busca.jsonp?nome=" + teamQuery;
			$.ajax({
				type: 'POST',
				url: query,
				crossDomain: true,
				cache: false,
				success: function(data) {
					console.log(data);
				}
			});
		}
		e.preventDefault();
	});*/
});